steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/proudofmom-be:$_PR_NUMBER', '.']
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/proudofmom-be:$_PR_NUMBER']
  # Deploy container image to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args: ['run', 'deploy', 'proudofmom', '--image', 'gcr.io/$PROJECT_ID/proudofmom-be:$_PR_NUMBER', '--region', 'us-central1', '--platform', 'managed', '--allow-unauthenticated', '--add-cloudsql-instances', 'rock-fountain-288922:us-central1:proudofmomps' , '--update-env-vars', 'INSTANCE_CONNECTION_NAME=rock-fountain-288922:us-central1:proudofmomps;DB_USER=proudofmomps_usr;DB_PASS=zipqlks83hxb7GBi;DB_NAME=proudofmomps_dev']
images:
- gcr.io/$PROJECT_ID/proudofmom-be:$_PR_NUMBER